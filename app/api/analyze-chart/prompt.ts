export const getChartAnalysisPrompt = (chartType: string, text: string) => {
    return `分析用户内容中的多维度数据并生成图表配置方案，要求如下:
  
  首先提炼出用户信息中适合以图表形式表示的多个维度数据，不要增加不存在的数据

  期望图表类型为: ${chartType || '自动推荐'}
  
  请分析数据特征并提供:
  1. 最适合的图表类型和配置
  2. 数据处理建议
  3. 视觉样式方案
  4. 关键数据洞察
  
  要求:
  - 根据数据特征选择合适的图表类型
  - 提供清晰的数据标签和图例
  - 选择协调的配色方案
  - 确保图表可读性和美观性
  - 突出关键数据特征和趋势
  
  用户提供内容:
  ${text}
  `;
  }